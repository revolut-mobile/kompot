ext {
    kompot_sample = [
            targetSdkVersion        : 31,
            compileSdkVersion       : 33,
            minSdkVersion           : 24,
            buildToolsVersion       : '33.0.2',
    ]
}